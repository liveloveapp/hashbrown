{
  "name": "invoicing-contracts",
  "projectType": "library",
  "sourceRoot": "samples/invoicing/shared/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p samples/invoicing/shared/tsconfig.json"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p samples/invoicing/shared/tsconfig.json && vitest run --config samples/invoicing/shared/vitest.config.mts"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint samples/invoicing/shared" }
    }
  }
}
