/**
 * The invoicing walkthrough video, hosted on Vercel Blob (store
 * `hashbrown-www-media`) rather than committed. Each recording is uploaded
 * under a content-hashed name, so this URL is immutable and cached for a
 * year; after `npx nx walkthrough invoicing-e2e`, replace it with the URL
 * that run prints.
 */
export const WALKTHROUGH_VIDEO_URL =
  'https://ue1aiy83g4k7z6ts.public.blob.vercel-storage.com/video/invoicing-walkthrough-fa5404934b91.mp4';
