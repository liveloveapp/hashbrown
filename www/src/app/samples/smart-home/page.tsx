import { RetiredSample } from '../../../components/samples/RetiredSample';

/** The retired Smart Home example (port of `samples/smart-home.page.ts`). */
export default function SmartHomeSamplePage() {
  return <RetiredSample name="Smart Home" />;
}
