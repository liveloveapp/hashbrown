import { RetiredSample } from '../../../components/samples/RetiredSample';

/** The retired Finance example (port of `samples/finance.page.ts`). */
export default function FinanceSamplePage() {
  return <RetiredSample name="Finance" />;
}
