import { RetiredSample } from '../../../components/samples/RetiredSample';

/** The retired Fast Food example (port of `samples/fast-food.page.ts`). */
export default function FastFoodSamplePage() {
  return <RetiredSample name="Fast Food" />;
}
