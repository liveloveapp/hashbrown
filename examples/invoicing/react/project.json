{
  "name": "invoicing-react",
  "projectType": "application",
  "sourceRoot": "examples/invoicing/react/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc --noEmit -p examples/invoicing/react/tsconfig.json",
          "vite build --config examples/invoicing/react/vite.config.mts"
        ],
        "parallel": false
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config examples/invoicing/react/vite.config.mts"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint examples/invoicing/react" }
    },
    "serve": {
      "executor": "nx:run-commands",
      "continuous": true,
      "options": {
        "command": "vite --config examples/invoicing/react/vite.config.mts"
      }
    }
  }
}
