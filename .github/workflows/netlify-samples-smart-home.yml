name: Netlify Smart Home Sample

on:
  pull_request:
    types: [labeled]

permissions:
  contents: read
  deployments: write

concurrency:
  group: netlify-samples-smart-home
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy smart-home sample to Netlify
    if: github.event_name == 'pull_request' && github.event.action == 'labeled' && github.event.label.name == 'samples/smart-home'
    runs-on: ubuntu-latest
    environment:
      name: smart-home-sample
      url: ${{ steps.deploy.outputs.production_url }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 22.x
          cache: npm

      - name: Determine affected base
        uses: nrwl/nx-set-shas@v3

      - name: Install dependencies
        run: npm ci

      - name: Build smart-home angular app and edge functions
        run: npx nx run smart-home-angular:build:production

      - name: Prepare Netlify configuration
        run: cp samples/smart-home/angular/netlify.toml netlify.toml

      - name: Deploy to Netlify
        id: deploy
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SMART_HOME_SITE_ID }}
        run: |
          npx netlify-cli deploy \
            --no-build \
            --auth "$NETLIFY_AUTH_TOKEN" \
            --site "$NETLIFY_SITE_ID" \
            --prod \
            --dir=dist/samples/smart-home/angular/browser \
            --message="Smart-home sample deploy for ${GITHUB_SHA}" \
            --json > deploy.json
          cat deploy.json
          PRODUCTION_URL=$(node -pe "require('./deploy.json').url || ''")
          echo "production_url=$PRODUCTION_URL" >> "$GITHUB_OUTPUT"
