name: Deploy Cloudflare Production

on:
  workflow_call:
    inputs:
      deploy_samples:
        description: 'Deploy the sample Cloudflare Pages projects'
        required: false
        type: boolean
        default: true
      deploy_www:
        description: 'Deploy the docs website Cloudflare Pages project'
        required: false
        type: boolean
        default: true
  workflow_dispatch:
    inputs:
      deploy_samples:
        description: 'Deploy the sample Cloudflare Pages projects'
        required: false
        type: boolean
        default: true
      deploy_www:
        description: 'Deploy the docs website Cloudflare Pages project'
        required: false
        type: boolean
        default: true
  push:
    branches:
      - cloudflare-production

concurrency:
  group: cloudflare-production
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      PRODUCTION_BRANCH: cloudflare-production
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: npm

      - name: Install
        run: npm ci

      - name: Build apps for Pages
        env:
          DEPLOY_SAMPLES: ${{ toJSON(inputs.deploy_samples) == 'null' || inputs.deploy_samples }}
          DEPLOY_WWW: ${{ toJSON(inputs.deploy_www) == 'null' || inputs.deploy_www }}
        run: |
          projects=()

          if [[ "$DEPLOY_SAMPLES" == "true" ]]; then
            projects+=(finance-angular fast-food-angular fast-food-cloudflare smart-home-angular)
          fi

          if [[ "$DEPLOY_WWW" == "true" ]]; then
            projects+=(www)
          fi

          if [[ "${#projects[@]}" -eq 0 ]]; then
            echo "At least one of deploy_samples or deploy_www must be true."
            exit 1
          fi

          npx nx run-many -t build --configuration=production -p "${projects[@]}"

      - name: Deploy Finance (production)
        if: ${{ toJSON(inputs.deploy_samples) == 'null' || inputs.deploy_samples }}
        working-directory: samples/finance/angular
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx wrangler pages deploy ../../../dist/samples/finance/angular/browser --project-name="hashbrown-finance" --branch="$PRODUCTION_BRANCH"

      - name: Deploy Smart Home (production)
        if: ${{ toJSON(inputs.deploy_samples) == 'null' || inputs.deploy_samples }}
        working-directory: samples/smart-home/angular
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx wrangler pages deploy ../../../dist/samples/smart-home/angular/browser --project-name="hashbrown-smart-home" --branch="$PRODUCTION_BRANCH"

      - name: Deploy Fast Food (production)
        if: ${{ toJSON(inputs.deploy_samples) == 'null' || inputs.deploy_samples }}
        working-directory: samples/fast-food/angular
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx wrangler pages deploy ../../../dist/samples/fast-food/angular/browser --project-name="hashbrown-fast-food" --branch="$PRODUCTION_BRANCH"

      - name: Deploy Docs (production)
        if: ${{ toJSON(inputs.deploy_www) == 'null' || inputs.deploy_www }}
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx wrangler pages deploy dist/www/analog/analog/public --project-name="hashbrown-www" --branch="$PRODUCTION_BRANCH"

      - name: Smoke test production deployments
        env:
          DEPLOY_SAMPLES: ${{ toJSON(inputs.deploy_samples) == 'null' || inputs.deploy_samples }}
          DEPLOY_WWW: ${{ toJSON(inputs.deploy_www) == 'null' || inputs.deploy_www }}
        run: |
          if [[ "$DEPLOY_WWW" == "true" ]]; then
            node scripts/deploy-smoke.mjs --name docs --url https://hashbrown.dev --expect Hashbrown --retries 12 --retry-delay-ms 5000
          fi

          if [[ "$DEPLOY_SAMPLES" == "true" ]]; then
            node scripts/deploy-smoke.mjs --name finance --url https://finance.hashbrown.dev --expect "Finance Sample" --retries 12 --retry-delay-ms 5000
            node scripts/deploy-smoke.mjs --name smart-home --url https://smart-home.hashbrown.dev --expect "Smart Home" --retries 12 --retry-delay-ms 5000
            node scripts/deploy-smoke.mjs --name fast-food --url https://fast-food.hashbrown.dev --expect "Fast Food Nutrition Sample" --retries 12 --retry-delay-ms 5000
          fi
